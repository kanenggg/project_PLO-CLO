apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ploclo
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30000
  type: LoadBalancer     # เปิดให้ external เข้าถึงได้